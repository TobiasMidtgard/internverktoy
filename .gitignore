# Local-only planning/design docs — kept OUT of the public repo on purpose
docs/

# Local tooling / config
.claude/
node_modules/
.env
.env.*

# Local one-time data backup (pre per-day-assignment wipe) — not for the public repo
db/assignees-backup.json
