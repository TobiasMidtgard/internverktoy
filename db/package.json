{
  "name": "db-runner",
  "private": true,
  "type": "module",
  "description": "Runs supabase/*.sql migrations against Postgres via a connection string in db/.env",
  "dependencies": {
    "pg": "^8.13.1"
  }
}
